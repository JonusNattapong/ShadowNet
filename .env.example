# ตัวอย่างไฟล์ .env สำหรับการตั้งค่า environment variables
# คัดลอกไฟล์นี้เป็น .env และแก้ไขค่าตามที่ต้องการ

# การตั้งค่าสภาพแวดล้อม (development, testing, production)
SHADOWNET_ENV=development

# การตั้งค่าฐานข้อมูล PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_NAME=shadownet

# การตั้งค่า API
API_PORT=8000

# การตั้งค่า Elasticsearch
ELASTICSEARCH_URL=http://localhost:9200

# การตั้งค่า Grafana
GRAFANA_PASSWORD=admin

# การตั้งค่าการแจ้งเตือน
ALERT_EMAIL=admin@example.com
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=alert@example.com
SMTP_PASSWORD=smtppassword

# การตั้งค่าความปลอดภัย
ENABLE_EXPLOITS=false
AUTO_BLOCK_THRESHOLD=0.8

# การตั้งค่า logging
LOG_LEVEL=info
LOG_FILE=logs/shadownet.log
